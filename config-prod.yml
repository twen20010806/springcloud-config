server:
  port: 8001
spring:
  application:
    name: cloud-payment-service #微服务名称,决定了不要轻易改动
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://rm-bp1vliax9fp8j51155o.mysql.rds.aliyuncs.com:3306/springcloud
    username: rawandy
    password: Hcx20011002
eureka:
  client:
    #是否从EurekaServer抓取已有的注册信息,默认为true。单节点无所为,集群必须设置为true才能配合Ribbon使用负载均衡
    fetch-registry: true
    #表示是否将自己注册进入EurekaServer默认true
    register-with-eureka: true
    service-url:
      #defaultZone: http://eureka7001.com:7001/eureka/,http://eureka7002.com:7002/eureka/
      defaultZone: http://eureka7001.com:7001/eureka/
  instance:
    instance-id: payment8001
    prefer-ip-address: true
    #开发时设置小些,保证服务关闭后注册中心能即使提出服务
    #Eureka客户端向服务端发送心跳的时间间隔
    #lease-renewal-interval-in-seconds: 1
    #Eureka服务端在收到最后一次心跳后等待实践上线,单位为秒(默认90秒),超时将提出服务
    #lease-expiration-duration-in-seconds: 2
mybatis-plus:
  type-aliases-package: com.twen.springcloud.entities
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:mapper/*.xml